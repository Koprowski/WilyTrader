# WilyTrader 0.3.31

- Aligns the `Target Exit MC` submenu by its bottom edge near the menu trigger.
- Keeps the target-exit submenu scrolled to the bottom when it opens or is hovered.
- Leaves stop-loss submenu positioning and ordering unchanged.

## Validation

- `node --check extension\src\content.js`
- `node --check extension\src\userscript-wrapper.user.js`
