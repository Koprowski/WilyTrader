# WilyTrader 0.3.35

- Passes Axiom execution marker text through TradingView's top-level drawing `text` option so signed trade-size labels render on new chart flags.
- Keeps the existing marker style overrides for color, background, font size, and follow-up updates.

## Validation

- `node --check extension\src\content.js`
- `node --check extension\src\userscript-wrapper.user.js`
- `git diff --check -- extension/src/content.js extension/src/axiomChartBridge.ts extension/src/userscript-wrapper.user.js extension/manifest.json release-notes/0.3.35.md`
