# WilyTrader 0.3.25

- Consolidates the 100% sell-button market-cap target update into the latest extension package.
- Keeps the visible `MC` control next to the `100%` sell button for left-click target/stop menu access.
- Updates the Desktop last-session folder wording to make the folder link more explicit.

## Validation

- `node --check extension\src\content.js`
- `node --check extension\src\userscript-wrapper.user.js`
- `npm run typecheck` in `desktop/`
- `npm run build` in `desktop/`
