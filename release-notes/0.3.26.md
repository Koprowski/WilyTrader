# WilyTrader 0.3.26

- Removes the extra `MC` sell-button control.
- Restores the sell grid so the `100%` button looks like the other sell buttons.
- Attaches the 100% target/stop menu directly to the `100%` button right-click event.
- Keeps exit-target diagnostics under `[WilyTrader][exit-target]`.

## Validation

- `node --check extension\src\content.js`
- `node --check extension\src\userscript-wrapper.user.js`
