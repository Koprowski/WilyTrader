# WilyTrader 0.3.29

- Reverses the `Target Exit MC` option order so higher market-cap targets are above lower targets.
- Opens the target-exit submenu scrolled to the bottom, with the nearest above-current target visible first.
- Keeps stop-loss options unchanged, with lower market caps reached by scrolling down.

## Validation

- `node --check extension\src\content.js`
- `node --check extension\src\userscript-wrapper.user.js`
