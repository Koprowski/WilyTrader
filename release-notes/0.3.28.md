# WilyTrader 0.3.28

- Adds the right-click `Target Exit MC` menu to every sell percentage button.
- Uses the clicked sell button percentage when creating the target exit order.
- Keeps `Target Stop Loss MC` limited to the `100%` sell button.

## Validation

- `node --check extension\src\content.js`
- `node --check extension\src\userscript-wrapper.user.js`
