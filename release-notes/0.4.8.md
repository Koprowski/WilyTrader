# WilyTrader Release 0.4.8

- Bumps Desktop app version to `0.2.2` so installed `0.2.1` builds detect an available Desktop update.
- Adds the capture-folder picker and writable default capture root for packaged installs.
- Keeps `ohlc_screenshot` as a source-backed, row-specific trade-log hyperlink instead of filling it down from `defs`.
- Copies green/formula `defs` row 2 formulas down into matching master trading log columns while excluding `ohlc_screenshot`.
- Prevents archive backfill from rewriting archived `trade_log.xlsx` files.

Validation:

- `npm --prefix desktop run typecheck`
- `npm --prefix desktop run dist:win`
- Live master `BO` formula comparison: 20 checked, 0 mismatches
