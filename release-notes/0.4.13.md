# WilyTrader Release 0.4.13

- Keeps WilyTrader chart flags above candlesticks when the chart is zoomed out.
- Adds an enabled-by-default Desktop setting to sync the master trading log automatically after stopping and finalizing a recording session.
- Repairs OHLC screenshot hyperlinks in the master trading log so synced rows point at archived session folders after the session folder is moved.
- Refreshes the last completed session folder path after master sync archives a finalized session.

Validation:

- `npm --prefix desktop run typecheck`
- `npm --prefix desktop run build`
- `npm --prefix desktop run dist:win`
- `node --check extension\src\userscript-wrapper.user.js`
- `node --check desktop\resources\trade-sync\sync-master-trading-log.mjs`
- `desktop\node_modules\.bin\tsc --target es2020 --lib dom,es2020 --noEmit --strict false extension/src/axiomChartBridge.ts`
- Live master sync against `E:\Apps\WilyTrader Captures\master trading log.xlsx`
- Workbook XML scan confirming Master Trading Log OHLC screenshot links point under Archive and resolve to existing PNG files
