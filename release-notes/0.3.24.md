# WilyTrader 0.3.24

- Adds a visible `MC` control next to the `100%` sell button.
- Opens the 100% market-cap target and stop menu on normal left-click, bypassing Chrome/Axiom right-click context-menu handling.
- Keeps the right-click diagnostic logging from 0.3.23 so failures can still be traced from DevTools.

## Validation

- `node --check extension\src\content.js`
- `node --check extension\src\userscript-wrapper.user.js`
