# WilyTrader 0.3.21

- Fixes the WilyTrader `100%` sell-button right-click target/stop menu so it also works on the header/collapsed sell-all button.
- Makes average entry/exit and WT target/stop chart lines movable again.
- Avoids reapplying chart points to movable lines during bridge sync, which reduced the jumpy drag behavior seen in Axiom.
- Removes the hover gap between the target/stop menu and its market-cap submenu.

## Validation

- `node --check extension\src\content.js`
- `node --check extension\src\userscript-wrapper.user.js`
- Direct TypeScript check for `extension\src\axiomChartBridge.ts`

## Manual test status

- Needs live Chrome/Axiom reload test from the unpacked `extension\` folder.
